# Laufzeit-Abhängigkeiten von Launchpad Pro TAB Edition
# Installation:  python -m pip install -r requirements.txt
PySide6-Essentials>=6.8        # Qt 6: Qt Quick/QML-Oberfläche (GPU-gerendert)
numpy>=1.26                    # DSP: Mischen, Wellenform, Time-Stretch
sounddevice>=0.4.6             # Audio-Ausgabe über PortAudio (Windows: WASAPI, geringe Latenz)
soundfile>=0.12.1              # WAV/FLAC/OGG/AIFF/MP3 lesen, FLAC schreiben (libsndfile)
av>=12.0                       # FFmpeg: MP3, AAC/M4A, WMA, Opus, ALAC, AC3 … dekodieren
soxr>=0.3.7                    # hochwertiges Resampling
pycaw>=20240316 ; sys_platform == "win32"   # Windows-Systemlautstärke (Core Audio)
comtypes>=1.2 ; sys_platform == "win32"
